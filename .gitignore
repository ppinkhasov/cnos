# dependencies (regenerated by `npm install`)
node_modules/

# whisper.cpp models — large binaries; downloaded via the README
models/
*.bin

# local Claude Code settings (machine-specific)
.claude/settings.local.json

# logs / OS / editor cruft
*.log
npm-debug.log*
.DS_Store
.idea/
.vscode/
